entier:     test    <-    1
afficher("Bonjour, je suis le test " + test + "\n")
affiche_test((adresse)test)

fonction: affiche_test((adresse)entier nb_test)
    tant que: (valeur)nb_test < 5
        entier: a
        (valeur)nb_test <- (valeur)nb_test + 1
        afficher("Bonjour, je suis le test " + (valeur)nb_test + "\n")
        a <- 0
    si: (valeur)nb_test == 5
        afficher("jadore la programmation")
    retourner:


(valeur)vide: texte <- "test"
my_printf("Voici un %s\n", texte)